import type { InputHTMLAttributes, TextareaHTMLAttributes } from "react";
const style="w-full rounded-xl border border-slate-700 bg-slate-950/60 px-4 py-3 text-slate-100 placeholder:text-slate-500";
export function Field({label,hint,...props}:InputHTMLAttributes<HTMLInputElement> & {label:string;hint?:string}) {const id=props.id??props.name;return <div className="space-y-2"><label className="block text-sm text-slate-300" htmlFor={id}>{label}</label><input {...props} id={id} className={style} aria-describedby={hint?`${id}-hint`:undefined}/>{hint&&<p id={`${id}-hint`} className="text-xs text-slate-400">{hint}</p>}</div>;}
export function TextField({label,hint,...props}:TextareaHTMLAttributes<HTMLTextAreaElement> & {label:string;hint?:string}){const id=props.id??props.name;return <div className="space-y-2"><label className="block text-sm text-slate-300" htmlFor={id}>{label}</label><textarea rows={5} {...props} id={id} className={style} aria-describedby={hint?`${id}-hint`:undefined}/>{hint&&<p id={`${id}-hint`} className="text-xs text-slate-400">{hint}</p>}</div>;}
